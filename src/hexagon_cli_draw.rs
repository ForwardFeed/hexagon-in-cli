use crate::hexagon_grid::HexagonGrid;



pub fn draw(grid: &HexagonGrid){
    let (g_x, g_y) = grid.get_hex_c_size();
    for row_i in 0..g_x{
        for col_i in 0..g_y{
            
        }
    }
}